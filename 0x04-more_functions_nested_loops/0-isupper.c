#include<stdio.h>

/**
 *  main - check the code.
 *  Description:  a function that checks for uppercase character
 *  Return: always 0.
 */
int main(void)
{
	int c;
		int _putchar(char c);

	if (c >= 'A' && c <= 'Z')
	return (1);
	else
	return (0);
	printf("%d\n", c);

	return (0);
}
